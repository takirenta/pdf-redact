# Security Policy（セキュリティ方針）

## 🛡️ 情報公開の事故を防ぐ設計（Safety by Design）

このツールは、公文書開示請求の業務で起こりがちな事故
（黒塗り漏れ・黒塗りの復元・元ファイルの破壊）を**構造的に防ぐ**ことを
最優先に設計されています。クラウドや外部サーバーに依存せず、
通信の余地そのものがありません。

### 黒塗りは物理削除（復元不能）

- 保存時に `add_redact_annot()` + `apply_redactions(images=fitz.PDF_REDACT_IMAGE_REMOVE)`
  で、黒塗り領域の**テキスト・画像を PDF から物理的に削除**します。
- `garbage=4` + `deflate=True` で、削除されたデータの**未参照残骸もファイルから除去**します。
- 保存後の PDF からテキスト抽出・画像抽出しても、黒塗り部分の中身は
  取り出せません（テストで検証済み）。

### 元ファイルは絶対に変更しない

- 保存は常に**別名保存**です。元の PDF はバイト単位で不変であることを
  テスト（`test_original_file_is_not_modified`）で検証しています。

### 安全側に倒す削除（漏えい防止）

- 黒塗り領域と**一部でも交差**するテキスト行・画像は**全体を削除**します。
  行の途中や画像の一部だけ黒塗りした場合も、中身の一部が残ることはありません。

### 入力ファイルの拒否

- パスワード付き PDF・破損ファイル・PDF 形式でないファイルは
  **開かずに**日本語エラーメッセージで拒否します（テストで検証済み）。

### 完全オフライン

- ネットワーク通信を行うコードは存在しません。
- 依存ライブラリは PyMuPDF のみ。文書データが外部に出る経路はありません。

## バグ報告・脆弱性報告

- 一般的なバグ・機能要望: [GitHub Issues](https://github.com/takirenta/pdf-redact/issues)
- セキュリティに関わる問題を非公開で報告したい場合:
  GitHub の **Private vulnerability reporting** をご利用ください。
- **注意**: 実在の機密文書・個人情報を含む PDF を Issue や PR に添付しないでください。
  テスト用のダミーデータのみを使用してください。
